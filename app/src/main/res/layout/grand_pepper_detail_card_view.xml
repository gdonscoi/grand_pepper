<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/grand_pepper_detail_card_view"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    android:layout_margin="10dp"
    card_view:cardCornerRadius="4dp">

    <RelativeLayout
        android:id="@+id/container_grand_pepper_detail_card"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#000000">

        <ImageView
            android:id="@+id/image_background_card"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:adjustViewBounds="true"
            android:contentDescription="@android:string/untitled"
            android:scaleType="fitXY"
            android:src="@drawable/galera"
            android:transitionName="comum_image_detail" />

        <!--<RelativeLayout-->
            <!--android:id="@+id/content_background_text"-->
            <!--android:layout_width="match_parent"-->
            <!--android:layout_height="wrap_content"-->
            <!--android:layout_alignBottom="@id/image_background_card"-->
            <!--android:background="#80000000"-->
            <!--android:transitionName="comum_background_text_detail">-->

            <TextView
                android:id="@+id/text_description_card"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="10dp"
                android:textColor="#ffffff"
                android:background="#80000000"
                android:layout_alignBottom="@id/image_background_card"
                android:textSize="26sp"
                android:transitionName="comum_text_detail" />
        <!--</RelativeLayout>-->
    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/content_background_disabled"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#80ffffff"
        android:visibility="gone">

    </RelativeLayout>
</android.support.v7.widget.CardView>